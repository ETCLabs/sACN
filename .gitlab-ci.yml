workflow:
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "web"'

variables:
  GIT_STRATEGY: clone
  GIT_SUBMODULE_STRATEGY: normal
  GLOBAL_CMAKE_OPTIONS: -DSACN_BUILD_TESTS=ON -DSACN_BUILD_EXAMPLES=ON -DSACN_WARN_AS_ERROR=ON
  KLOCWORK_PROJECT: "sACN"

include:
  # Versioned build automation
  - project: "etc/common-tech/tools/gitlab-ci-util"
    ref: v2.3.1
    file: "/ci-yml/deploy-versioned-build.yml"

windows-build:
  stage: build
  tags:
    - ct-windows
  variables:
    SANITIZER_DEFS: -DSACN_ENABLE_E2E_TESTS=ON  # TODO: Fix & re-enable -DSACN_ENABLE_ASAN=ON
  script:
    - mkdir build
    - cd build
    - cmake -G "Visual Studio 16 2019" -A x64 $env:GLOBAL_CMAKE_OPTIONS.Split(" ") $env:SANITIZER_DEFS.Split(" ") ..
    - cmake --build . --config Release -j
    - ctest -C Release --output-on-failure
  artifacts:
    when: always
    reports:
      junit: build/tests/test-results/*.xml

macos-build:
  stage: build
  tags:
    - ct-mac-m2
  parallel:
    matrix:
      - SANITIZER_DEFS:
          - -DSACN_ENABLE_E2E_TESTS=ON -DSACN_ENABLE_ASAN=ON -DSACN_ENABLE_UBSAN=ON
          - -DSACN_ENABLE_E2E_TESTS=ON -DSACN_ENABLE_TSAN=ON
  script:
    - mkdir build
    - cd build
    - cmake ${GLOBAL_CMAKE_OPTIONS} ${SANITIZER_DEFS} ..
    # TEMPORARY (flaky CoverageTest investigation): build & run only the CoverageTest tests to speed up the pipeline.
    # To restore the full build/test, remove the three temporary commands below and un-comment the two ORIGINAL lines.
    - cmake --build . --target e2e_coverage_test_dynamic
    - cmake --build . --target e2e_coverage_test_static
    # Use -V (verbose) instead of --output-on-failure so the SACN-DIAG printouts ALWAYS show, even when a
    # run passes (--output-on-failure only dumps captured stdout for FAILED tests, which is why nothing showed).
    # Loop the run a few times to try to catch the flaky failure; stop at the first failing iteration.
    # Adjust the iteration count in the "for" list below; narrow to a single test with e.g. -R DetectSourcesComingAndGoing.
    - |
        for run in 1 2 3 4 5; do
          echo "================ DetectSourcesComingAndGoing loop iteration $run ================"
          ctest -V -R DetectSourcesComingAndGoing || exit 1
        done
    # --- ORIGINAL (re-enable when done) ---
    # - cmake --build .
    # - ctest --output-on-failure
  artifacts:
    when: always
    reports:
      junit: build/tests/test-results/*.xml

linux-gcc-build:
  stage: build
  tags:
    - etc-linux-docker
  image: etc-docker.artifactory.etcconnect.com/etc/common-tech/gcc-cmake:llvm18
  parallel:
    matrix:
      - SANITIZER_DEFS:
          - -DSACN_ENABLE_E2E_TESTS=ON -DSACN_ENABLE_ASAN=ON -DSACN_ENABLE_UBSAN=ON
          - -DSACN_ENABLE_E2E_TESTS=ON -DSACN_ENABLE_TSAN=ON
  script:
    - mkdir build
    - cd build
    - cmake ${GLOBAL_CMAKE_OPTIONS} ${SANITIZER_DEFS} ..
    - make -j `nproc`
    - ctest --output-on-failure
  artifacts:
    when: always
    reports:
      junit: build/tests/test-results/*.xml

linux-clang-build:
  stage: build
  tags:
    - etc-linux-docker
  image: etc-docker.artifactory.etcconnect.com/etc/common-tech/clang-cmake-msan:latest
  parallel:
    matrix:
      - SANITIZER_DEFS:
          - -DSACN_ENABLE_E2E_TESTS=ON -DSACN_ENABLE_MSAN=ON
  script:
    - mkdir build
    - cd build
    - cmake ${GLOBAL_CMAKE_OPTIONS} ${SANITIZER_DEFS} ..
    - make -j `nproc`
    - ctest --output-on-failure
  artifacts:
    when: always
    reports:
      junit: build/tests/test-results/*.xml

oss-build:
  stage: build
  tags:
    - etc-linux-docker
  image: etc-docker.artifactory.etcconnect.com/etc/common-tech/gcc-cmake:llvm18
  script:
    # Remove the .git folder to simulate building from P4/zip archive
    - rm -rf .git/
    - mkdir build
    - cd build
    # Use default config, no options
    - cmake ..
    - make -j `nproc`

# Job to lint the code using clang-tidy and clang-format.
lint:
  stage: build
  tags:
    - etc-linux-docker
  variables:
    CMAKE_OPTIONS: -DSACN_ENABLE_CLANG_TIDY=ON -DSACN_CLANG_TIDY_WERROR=ON
  image: etc-docker.artifactory.etcconnect.com/etc/common-tech/gcc-cmake:llvm18
  script:
    - mkdir build
    - cd build
    - cmake ${GLOBAL_CMAKE_OPTIONS} ${CMAKE_OPTIONS} ..
    - make check_formatting
    - make -j `nproc`

docs:
  stage: build
  allow_failure: true
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "web"'
  tags:
    - etc-linux-docker
  image: etc-docker.artifactory.etcconnect.com/etc/common-tech/doxygen:1.11.0
  variables:
    GIT_DEPTH: 0
  script:
    - git fetch --tags
    - etcdevtool docs . 1.11.0
    - mkdir docs-build
    - mv docs/build/* docs-build/
  artifacts:
    paths:
      - docs-build

deploy-docs:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
  tags:
    - etc-linux-docker
  image: etc-docker.artifactory.etcconnect.com/etc/common-tech/doxygen:1.11.0
  variables:
    GIT_DEPTH: 0
    GH_REPO_NAME: sACNDocs
    GH_REPO_REF: github.com/ETCLabs/sACNDocs.git
    GH_REPO_TOKEN: $SVC_ETCLABS_REPO_TOKEN
  script:
    - pip install --upgrade etcdevtool
    - chmod +x tools/ci/publish_docs.sh
    - tools/ci/publish_docs.sh
